import { MetricsPanel } from '@/components/admin/MetricsPanel';

/** Route `/admin/apercu` — métriques et files exploitables. */
export default function AdminOverviewPage() {
  return <MetricsPanel />;
}
