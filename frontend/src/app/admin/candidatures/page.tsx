'use client';import{useQuery}from'@tanstack/react-query';import{api}from'@/lib/opsApi';import{Card,Badge}from'@/components/ui';import type{Page,JobApplication}from'@/lib/candidateMarketplace';type Row=JobApplication&{candidate_profile:{first_name:string|null;last_name:string|null;user:{phone:string}}};export default function AdminApplications(){const q=useQuery({queryKey:['admin-applications'],queryFn:()=>api.get('/admin/applications').then(r=>r.data as Page<Row>)});return <main className="mx-auto grid max-w-6xl gap-4 p-6"><h1 className="text-2xl font-bold">Toutes les candidatures</h1>{q.data?.data.map(a=><Card key={a.id}><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-bold">{a.candidate_profile?.first_name} {a.candidate_profile?.last_name}</h2><p className="helper-text">{a.offer.title} · {a.offer.city} · {a.candidate_profile?.user?.phone}</p></div><Badge tone={a.status==='accepted'?'done':'pending'}>{a.status}</Badge></div></Card>)}</main>}
