import AgentDashboard from '@/components/AgentDashboard';

/** Route `/agent` — QR de parrainage et suivi des commissions, portés depuis
 *  web-admin. */
export default function AgentPage() {
  return <AgentDashboard />;
}
