import { CandidateShell } from '@/components/amud/CandidateShell';

export default function AmudCandidateLayout({ children }: { children: React.ReactNode }) {
  return <CandidateShell>{children}</CandidateShell>;
}
