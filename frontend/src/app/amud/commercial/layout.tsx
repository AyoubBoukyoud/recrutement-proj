import { CommercialShell } from '@/components/amud/CommercialShell';

export default function AmudCommercialLayout({ children }: { children: React.ReactNode }) {
  return <CommercialShell>{children}</CommercialShell>;
}
