'use client';import Link from'next/link';import{usePathname}from'next/navigation';
const nav=[['/recruiter','Candidats'],['/recruiter/offres','Offres'],['/recruiter/candidatures','Candidatures'],['/recruiter/notifications','Notifications']];export default function Layout({children}:{children:React.ReactNode}){const p=usePathname();return <div><nav className="flex gap-2 border-b bg-surface-lowest px-6 py-3">{nav.map(([href,label])=><Link key={href} href={href} className={`rounded-full px-4 py-2 text-sm font-bold ${p===href?'bg-primary text-onPrimary':'text-primary hover:bg-primary/5'}`}>{label}</Link>)}</nav>{children}</div>}
