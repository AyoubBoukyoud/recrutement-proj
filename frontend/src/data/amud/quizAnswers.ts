/** Réexport fin — voir `quizTypes.ts` (types). Collection purement générée par l'usage (réponses en direct), jamais pré-seedée. */
export type { QuizAnswer } from './quizTypes';
export const quizAnswersSeed: import('./quizTypes').QuizAnswer[] = [];
