/** Réexport fin — voir `quizTypes.ts` (types). Collection purement générée par l'usage (rejoindre un quiz), jamais pré-seedée. */
export type { QuizParticipant, QuizParticipantStatus } from './quizTypes';
export const quizParticipantsSeed: import('./quizTypes').QuizParticipant[] = [];
