/** Réexport fin — voir `quizTypes.ts` (types). Collection purement générée par l'usage (fin de quiz), jamais pré-seedée. */
export type { QuizResult } from './quizTypes';
export const quizResultsSeed: import('./quizTypes').QuizResult[] = [];
