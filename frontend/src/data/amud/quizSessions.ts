/** Réexport fin — voir `quizTypes.ts` (types). Collection purement générée par l'usage (lancement d'un quiz), jamais pré-seedée. */
export type { QuizSession, QuizSessionStatus } from './quizTypes';
export const quizSessionsSeed: import('./quizTypes').QuizSession[] = [];
