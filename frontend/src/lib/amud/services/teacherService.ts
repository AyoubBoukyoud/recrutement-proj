/** Voir `storageService.ts` pour le principe de cette couche. */
export {
  centerTeachersCollection as teachersCollection,
  loadLocalCenterTeachers as loadTeachers,
  addLocalCenterTeacher as addTeacher,
  updateLocalCenterTeacher as updateTeacher,
  removeLocalCenterTeacher as removeTeacher,
} from '../localCenterTeachers';
