# API Laravel consommée par l'authentification (OTP WhatsApp). Doit être joignable
# depuis le navigateur du candidat, pas seulement depuis le conteneur : sur un
# téléphone du réseau local, mettez l'IP de la machine plutôt que localhost.
NEXT_PUBLIC_API_URL=http://localhost:8000/api
