import { ReferralPayouts } from '../../components/ReferralPayouts'

/** Route `/admin/parrainage` — le versement des commissions. */
export function AdminCommissions() {
  return <ReferralPayouts />
}
