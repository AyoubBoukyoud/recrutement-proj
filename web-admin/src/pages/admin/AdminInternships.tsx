import { TaskCataloguePanel } from '../../components/admin/TaskCataloguePanel'

/** Route `/admin/stage` — le catalogue d'activités du stage quotidien. */
export function AdminInternships() {
  return <TaskCataloguePanel />
}
